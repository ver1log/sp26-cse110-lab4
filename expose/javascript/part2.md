1. At line 12 there will be no error and it will print 3 becuase they used var which is function scope, and prices is length 3
2. This will print 150 which is the last discount that was applied in the loop and that is what is stored in the variable
3. This is again 150 because the last time final price is updated it stored the value of 150 before the loop exited
4. The function will return an array of the discounted prices that were push during the for loop 
5. Error, i was defined in the for loop scope
6. Error, discountedPrice was defined in for loop scope
7. 150 since we defined it in the outer most level it has function scope
8. This will again return the array of discounted prices, and it has function scope
9. Out of scope error for the variable i
10. prints 3, since it hasnt changed after declaration
11. It returns the list of the discounted prices. 
12. 
A. student.name
B. student['Grad Year']
C. student.greeting()
D. student['Favorite Teacher'].name 
13. 
A. '32', concatnation
B. 1, treats it as arithmetic
C. 3, null is treated as 0
D. '3null', concatnates
E. 4, true treated as 1
F. 0, null is treated as 1 and addition between booleans defaults to integer 
G. '3undefined', concatnation
H. NaN, invalid mathmatical operation
14. 
A. true, 2 is converted to an integer
B. false, letter by letter comparison
C. true, loose comparision, converts
D. false, strict comparision and their types are not the same 
E. false, true is converted to an integer and it is not equal
F. true, anything other than 0 is true, so they are equal when converted
15. The difference between == and === is that the === takes into account the type in the comparision and will not say they are equal if they have different types
16. 
17. [ 2, 4, 6 ], due to the callback function multiplying all the inputs by 2. The callback function that is passed in is doSomething which is called from inside. 
18. 
19. 1 4 3 2
